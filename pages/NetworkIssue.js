import {Text, ScrollView} from 'react-native';

const NetworkIssue = () => {
  return (
    <ScrollView>
      <Text>Net Work Not Found..!!</Text>
    </ScrollView>
  );
};

export default NetworkIssue;
